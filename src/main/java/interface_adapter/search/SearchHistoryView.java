package interface_adapter.search;

/**
 * Interface for displaying the search history in the view.
 */
public interface SearchHistoryView {
    @SuppressWarnings({"checkstyle:MissingJavadocMethod", "checkstyle:SuppressWarnings"})
    void updateHistoryView(String text);
}
